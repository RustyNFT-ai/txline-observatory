services:
  - type: web
    name: txline-observatory
    runtime: python
    plan: free
    buildCommand: python3 -m py_compile server.py normalize.py
    startCommand: python3 server.py
    healthCheckPath: /api/health
